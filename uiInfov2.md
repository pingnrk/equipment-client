# รายละเอียดการทำงานของ UI ระบบยืมครุภัณฑ์ (ฝั่ง Angular)

## Pages & Routing: โครงสร้างหน้าเว็บ

ระบบมีหน้าหลักๆ ดังนี้:

- **/login**: หน้าสำหรับเข้าสู่ระบบ
- **/register**: หน้าสำหรับลงทะเบียนผู้ใช้ใหม่
- **/equipments**: หน้าหลักสำหรับแสดงรายการและค้นหาครุภัณฑ์ (หน้าแรกของระบบ)
- **/cart**: หน้าตะกร้าสำหรับแสดงรายการครุภัณฑ์ที่ผู้ใช้เลือกไว้เพื่อขอยืม
- **/my-history**: หน้าประวัติการยืม-คืนครุภัณฑ์ของผู้ใช้
- **/track-requests**: หน้าสำหรับติดตามสถานะการขอยืมครุภัณฑ์
- **/admin-dashboard**: หน้าแดชบอร์ดสำหรับผู้ดูแลระบบ
- **/admin-users**: หน้าสำหรับจัดการข้อมูลผู้ใช้ในระบบ (สำหรับ Admin)
- **/admin-categories**: หน้าสำหรับจัดการหมวดหมู่ของครุภัณฑ์ (สำหรับ Admin)
- **/admin-equipment-list**: หน้าสำหรับแสดงและจัดการรายการครุภัณฑ์ทั้งหมด (สำหรับ Admin)
- **/add-equipment**: หน้าสำหรับเพิ่มครุภัณฑ์ใหม่ (สำหรับ Admin)
- **/admin-manage-requests**: หน้าสำหรับอนุมัติหรือจัดการคำขอยืมครุภัณฑ์ (สำหรับ Admin)
- **/admin-reports**: หน้าสำหรับดูรายงานสรุปต่างๆ (สำหรับ Admin)

## Core Features: ฟีเจอร์หลัก

- **ระบบสมาชิก:**
    - การเข้าสู่ระบบ (Login) และออกจากระบบ (Logout)
    - การลงทะเบียน (Register)
    - การแยก Role ระหว่าง `user` และ `admin` เพื่อกำหนดสิทธิ์การเข้าถึง
- **ระบบการยืม-คืน:**
    - ผู้ใช้สามารถค้นหาและดูรายละเอียดครุภัณฑ์ได้
    - **ระบบตะกร้า (Cart):** ผู้ใช้สามารถเพิ่มครุภัณฑ์ที่ต้องการลงในตะกร้า
    - **ฟอร์มขอยืม:** ผู้ใช้สามารถส่งคำขอยืมจากรายการในตะกร้า พร้อมระบุวันที่ต้องการยืมและคืน
    - **การติดตามสถานะ:** ผู้ใช้สามารถดูสถานะคำขอยืมของตนเองได้ (เช่น รอดำเนินการ, อนุมัติแล้ว)
    - **ประวัติการยืม:** ผู้ใช้สามารถดูประวัติการยืม-คืนของตนเองได้
- **ระบบจัดการสำหรับ Admin:**
    - Dashboard สรุปข้อมูลภาพรวม
    - จัดการผู้ใช้, ครุภัณฑ์, และหมวดหมู่
    - อนุมัติ, ปฏิเสธ หรือจัดการคำขอยืมจากผู้ใช้

## Form & Validation: การจัดการฟอร์ม

- **Reactive Forms:** โครงการใช้ `ReactiveFormsModule` ของ Angular ในการจัดการฟอร์ม ทำให้สามารถจัดการสถานะและข้อมูลของฟอร์มได้อย่างมีประสิทธิภาพ
- **Validators:** มีการใช้ `Validators` สำหรับตรวจสอบความถูกต้องของข้อมูล (Data Validation) ในฟอร์มต่างๆ เช่น:
    - `Validators.required`: ตรวจสอบว่าผู้ใช้กรอกข้อมูลในช่องนั้นๆ หรือไม่
    - `Validators.email`: ตรวจสอบว่ารูปแบบของอีเมลถูกต้องหรือไม่
- **ตัวอย่าง (หน้า Login):** ฟอร์ม Login จะมีช่อง `email` และ `password` ซึ่งถูกกำหนดให้เป็น `required` และช่อง `email` จะต้องมีรูปแบบที่ถูกต้องก่อนที่ผู้ใช้จะสามารถกดปุ่ม Submit ได้

## UI/UX Libraries: UI Framework

จากการตรวจสอบ `package.json` และโครงสร้างโปรเจกต์ ไม่พบการใช้งาน UI Library หรือ Framework ภายนอกที่เป็นที่นิยม เช่น Angular Material, Bootstrap, หรือ Tailwind CSS เป็นหลัก
ดังนั้น **UI ของโปรเจกต์จึงถูกสร้างขึ้นโดยใช้ความสามารถพื้นฐานของ Angular ร่วมกับการเขียน CSS แบบกำหนดเอง (Custom CSS)** ในแต่ละ Component เพื่อสร้างหน้าตาและประสบการณ์การใช้งาน (UX) ของเว็บแอปพลิเคชัน
